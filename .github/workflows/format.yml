name: format

on: [push, pull_request]

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1

    - name: Set up Python
      uses: actions/setup-python@v1

    - name: Install dependencies
      run: pip3 install .[dev]

    - name: Run tests
      run: |
        # pylint --rcfile=setup.cfg main.py tests setup.py
        isort --check-only --diff --recursive main.py tests setup.py
        yapf --diff --recursive main.onlinejudge_api tests setup.py | tee yapf.patch && test ! -s yapf.patch
        mypy main.py tests setup.py
